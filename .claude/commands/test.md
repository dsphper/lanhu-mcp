Run the project's test suite with pytest.

Execute all tests in the `tests/` directory with verbose output and coverage report.

```bash
python -m pytest tests/ -v --cov=. --cov-report=term-missing
```
